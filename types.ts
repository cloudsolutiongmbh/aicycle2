export interface TeamMember {
  name: string;
  role: string;
  description: string;
  image: string;
}

export interface Advisor {
  name: string;
  role: string;
  image: string;
}

export interface Phase {
  number: string;
  title: string;
  description: string;
}